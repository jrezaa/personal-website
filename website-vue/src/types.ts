export type Friend = {
  id: string;
  name: string;
  phone: string;
  email: string;
  isFavourite: boolean;
};

export type Resource = {
  title: string;
  description: string;
  link: string;
  id: string;
};
